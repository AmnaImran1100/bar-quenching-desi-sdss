numpy>=1.24
pandas>=2.0
scipy>=1.10
astropy>=5.3
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=12.0        # parquet engine
tqdm>=4.65

# Optional. The partial correlation (Method C) and Wilson intervals
# (Method B) are implemented by hand in src/ so the pipeline has NO hard
# dependency on these. Install them only if you want to cross-check the
# hand-rolled statistics against a reference implementation.
# pingouin>=0.5      # reference partial correlation
# statsmodels>=0.14  # reference Wilson proportion_confint
